[project]
name = "glpi-mcp"
version = "0.1.0"
description = "Servidor MCP para consultas ao banco MariaDB do GLPI"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "mcp[cli]>=1.27.2",
]
